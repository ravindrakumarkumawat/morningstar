import 'package:flutter/material.dart';

class TimelineSetting extends StatelessWidget {
  const TimelineSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Timeline settings'),
      )
    );
  }
}